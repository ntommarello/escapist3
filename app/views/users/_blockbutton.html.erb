<div id="BlockButton" style="margin-top:5px; float:left; margin-left:4px; width:106px" class="ProfileButton" onclick="<%if !current_user%>openRegister('Sign up for <%=APP_NAME%> to Block <%=@user.first_name%>')<%else%>blockUser(this,<%=user.id%>,0,'<%=user.first_name%>')<%end%>"><%if current_user and current_user.has_blocked?(user)%>Unblock<%else%>Block<%end%></div>
<div  style="margin-top:5px; float:left; margin-left:-1px; width:32px;padding:0px;" class="ProfileButton FlagButton" onclick="<%if !current_user%>openRegister('Sign up for <%=APP_NAME%> to Block <%=@user.first_name%>')<%else%>blockUser(this,<%=user.id%>,1,'<%=user.first_name%>')<%end%>">
</div>